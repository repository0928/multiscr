# 多科目雲端評分系統 — 計畫書 v2

## 一、系統概覽

建立一個完整的 Web 應用，讓老師可以：
- 登入個人帳號（中文一字帳號 + 4字元密碼）
- 自建多個**科目**（如：國語、數學…）
- 管理**學生名單**（全系統共用，CSV 批量匯入匯出）
- 自訂各科目的**評分項目**（名稱 + 配分，可排序，CSV 匯入匯出）
- 用**滑桿**為每位學生評分並即時儲存
- 在**總覽**頁看分數表、等第，並匯出 xlsx 試算表

---

## 二、技術架構

```
multiscr/
├── client/                  ← Vue 3 + Vite + Tailwind CSS（前端 SPA）
│   ├── src/
│   │   ├── views/
│   │   │   └── LoginView.vue        登入頁
│   │   ├── components/
│   │   │   ├── SubjectBar.vue       科目下拉 + 新增/改名/刪除
│   │   │   ├── StudentTab.vue       學生管理
│   │   │   ├── ItemTab.vue          評分項目管理
│   │   │   ├── ScoringTab.vue       評分（滑桿）
│   │   │   ├── OverviewTab.vue      成績總覽 + 匯出
│   │   │   └── AccountModal.vue     修改帳號/密碼
│   │   ├── stores/                  Pinia 全域狀態
│   │   └── api/                     axios 封裝（附 JWT 攔截器）
│   ├── package.json
│   └── vite.config.js
│
├── server/                  ← Node.js + Express + Prisma ORM（後端 API）
│   ├── src/
│   │   ├── routes/
│   │   │   ├── auth.js              登入 / 修改帳密
│   │   │   ├── subjects.js
│   │   │   ├── students.js
│   │   │   ├── items.js
│   │   │   └── scores.js
│   │   ├── middleware/
│   │   │   └── authMiddleware.js    JWT 驗證
│   │   └── app.js
│   ├── prisma/
│   │   └── schema.prisma
│   └── package.json
│
└── README.md
```

### 選用理由
| 層次 | 技術 | 原因 |
|------|------|------|
| 前端 | Vue 3 + Vite | 輕量快速，適合此規模的 SPA |
| 樣式 | Tailwind CSS | 彈性高，能還原截圖 UI 風格 |
| 狀態 | Pinia | Vue 官方推薦，比 Vuex 簡潔 |
| 後端 | Express (Node.js) | 輕量、Zeabur 原生支援 |
| ORM | Prisma | Schema 易讀，migrate 方便 |
| 資料庫 | PostgreSQL | Zeabur 內建提供，生產等級穩定 |
| 認證 | JWT (jsonwebtoken) | 無狀態、前後端分離友好 |
| 部署 | GitHub + Zeabur | 推 git 即自動部署 |

---

## 三、資料庫設計

```
teachers（老師帳號）
  id          SERIAL PK
  username    VARCHAR(10)   -- 中文一字，系統唯一
  password    VARCHAR(255)  -- bcrypt 雜湊儲存
  created_at  TIMESTAMP

subjects（科目）
  id          SERIAL PK
  teacher_id  FK → teachers.id   -- 每位老師有自己的科目
  name        VARCHAR(50)
  created_at  TIMESTAMP

students（學生 — 跨科共用，屬於老師）
  id          SERIAL PK
  teacher_id  FK → teachers.id
  year        SMALLINT      -- 年
  class       SMALLINT      -- 班
  number      SMALLINT      -- 號
  name        VARCHAR(50)   -- 姓名
  created_at  TIMESTAMP

scoring_items（評分項目，每科獨立）
  id          SERIAL PK
  subject_id  FK → subjects.id
  name        VARCHAR(50)
  max_score   SMALLINT
  order_index SMALLINT
  created_at  TIMESTAMP

scores（分數）
  id          SERIAL PK
  subject_id  FK → subjects.id
  student_id  FK → students.id
  item_id     FK → scoring_items.id
  score       SMALLINT
  UNIQUE(subject_id, student_id, item_id)
```

> **資料隔離**：每位老師只能看到、操作自己的科目與學生；JWT 攜帶 `teacher_id`，後端每支 API 都會驗證擁有權。

---

## 四、登入與帳號功能

### 登入規則
- 帳號：中文，**1個字**（例：「豪」）
- 密碼：**4個字元**（字母、數字或符號皆可）
- 登入成功後發給 JWT（有效期 7 天），存在 localStorage

### 帳號設定（登入後可修改）
- 修改帳號名稱（中文 1 字，不能與其他老師重複）
- 修改密碼（輸入新密碼 4 字元）
- 密碼以 bcrypt 雜湊儲存，不明文

### 初始老師建立
- 系統啟動後，第一次進入若無帳號，顯示「建立第一個帳號」頁面
- 或透過後台 API 預先建立（server/.env 設定初始帳密）

---

## 五、API 規格

### 認證
| Method | Path | 說明 |
|--------|------|------|
| POST | `/api/auth/login` | 登入，回傳 JWT |
| PUT | `/api/auth/profile` | 修改帳號 / 密碼 (需 JWT) |

### 科目
| Method | Path | 說明 |
|--------|------|------|
| GET | `/api/subjects` | 列出我的科目 |
| POST | `/api/subjects` | 新增科目 |
| PUT | `/api/subjects/:id` | 改名 |
| DELETE | `/api/subjects/:id` | 刪除（連同其項目與分數） |

### 學生（跨科共用）
| Method | Path | 說明 |
|--------|------|------|
| GET | `/api/students` | 列出所有學生 |
| POST | `/api/students` | 手動新增一名學生 |
| DELETE | `/api/students/:id` | 刪除學生 |
| DELETE | `/api/students` | 批次刪除（body: ids[]） |
| POST | `/api/students/import` | CSV 批量匯入（年,班,號,姓名） |
| GET | `/api/students/export` | 匯出學生 CSV |

### 評分項目
| Method | Path | 說明 |
|--------|------|------|
| GET | `/api/subjects/:id/items` | 列出本科項目 |
| POST | `/api/subjects/:id/items` | 新增項目 |
| PUT | `/api/subjects/:id/items/:itemId` | 編輯名稱/配分/排序 |
| DELETE | `/api/subjects/:id/items/:itemId` | 刪除項目 |
| POST | `/api/subjects/:id/items/import` | CSV 匯入（名稱,滿分） |
| GET | `/api/subjects/:id/items/export` | 匯出項目 CSV |

### 分數
| Method | Path | 說明 |
|--------|------|------|
| GET | `/api/subjects/:id/scores` | 取得本科所有分數 |
| PUT | `/api/subjects/:id/scores` | 批次 upsert 分數 |
| GET | `/api/subjects/:id/export` | 匯出成績 xlsx |

---

## 六、前端功能細節

### 登入頁
- 輸入帳號（中文 1 字）+ 密碼（4 字元）→ 登入
- 若系統尚無任何老師 → 顯示「建立帳號」頁

### 科目列（頂部，登入後）
- Dropdown 選擇科目、➕ 新增、✏️ 改名、🗑️ 刪除
- 右上角：老師名稱 + 帳號設定按鈕 + 登出

### 學生 Tab
- 批量匯入：選擇 CSV（格式：`年,班,號,姓名`）→ 執行匯入
- 手動新增：輸入年 / 班 / 號 / 姓名 → OK
- 清單：全選 / 單選 checkbox、篩選班級、批次刪除
- 匯出學生 CSV

### 項目 Tab
- 輸入項目名稱 + 滿分 → 新增
- 清單：↑↓ 排序、🗑️ 刪除
- 匯入 / 匯出 CSV（格式：`名稱,滿分`）

### 評分 Tab
- 篩選班級 input
- 每位學生一張卡片，顯示各項滑桿（0 到 max_score）
- 即時顯示總分 / 滿分；自動標示「滿分」徽章
- 滑桿放開後 debounce 500ms 自動儲存

### 總覽 Tab
- 表格：學生 | 項目1分 | 項目2分 | … | 總分 | 等第
- 等第：優(90–100) 甲(80–89) 乙(70–79) 丙(60–69) 丁(<60)
- 百分比以「學生總分 / 本科滿分總計」計算
- 匯出 `.xlsx`（使用 ExcelJS）

---

## 七、匯入 / 匯出格式

### 學生 CSV（`students.csv`）
```
年,班,號,姓名
1,1,1,曾小豪
1,1,2,賴小德
```

### 評分項目 CSV（`items.csv`）
```
名稱,滿分
做完,50
做對,30
做好,20
```

### 成績試算表（`.xlsx`）
| 學生 | 做完 | 做對 | 做好 | 總分 | 等第 |
|------|------|------|------|------|------|
| 1-1 曾小豪 | 50 | 15 | 20 | 85 | 甲 |

---

## 八、Zeabur 部署方案

```
GitHub 倉庫 (multiscr)
  └── 推送 main branch → Zeabur 自動部署
        ├── Service 1: client  → Static Site（前端，build output）
        ├── Service 2: server  → Node.js（後端 API，PORT 由 Zeabur 注入）
        └── Service 3: PostgreSQL（Zeabur 內建，DATABASE_URL 自動注入）
```

**環境變數：**
| 變數 | 位置 | 說明 |
|------|------|------|
| `DATABASE_URL` | server | Zeabur 自動注入 |
| `JWT_SECRET` | server | 自設安全金鑰 |
| `VITE_API_URL` | client build | 後端 API 網址 |

**部署步驟（執行後說明）：**
1. 建立 GitHub 倉庫，推送程式碼
2. Zeabur 建立 Project，連接 GitHub repo
3. 加入 PostgreSQL 服務，設定環境變數
4. 每次推送 main branch 即自動重新部署

---

## 九、開發階段規劃

| 階段 | 內容 |
|------|------|
| 1 | 建立 monorepo、Git 初始化、.gitignore、README |
| 2 | 後端：Prisma schema + migrate + 種子資料 |
| 3 | 後端：認證 API（登入、修改帳密）|
| 4 | 後端：科目 / 學生 / 項目 / 分數 CRUD API |
| 5 | 前端：登入頁 + JWT 攔截器 |
| 6 | 前端：科目列 + 四個 Tab 完整實作 |
| 7 | 匯入匯出（CSV / xlsx） |
| 8 | Zeabur 部署設定（zeabur.json） |
| 9 | 整合測試，UI 調整至符合截圖風格 |

---

**計畫書確認後即可開始執行，請回覆「確認，開始執行」！**
